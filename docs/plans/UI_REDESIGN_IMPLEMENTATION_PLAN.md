# UI重新设计实现计划

> **版本**: 1.0.0  
> **创建日期**: 2026-01-26  
> **最后更新**: 2026-01-26  
> **状态**: 主要功能已完成，待测试

## 📋 目录

1. [需求分析](#需求分析)
2. [影响范围](#影响范围)
3. [UI组件设计](#ui组件设计)
4. [实现方案](#实现方案)
5. [实现步骤](#实现步骤)
6. [注意事项](#注意事项)
7. [测试方案](#测试方案)

---

## 🎯 需求分析

### 用户目标
1. **现代化UI设计**：采用专业、商业化的深蓝灰色系设计
2. **统一资源管理**：本地文件、URL、挂载路径统一显示
3. **挂载路径支持**：支持挂载本地路径，自动扫描资源
4. **优化的控制界面**：更清晰的控制栏布局和时间显示

### 涉及功能模块
- **主窗口（MainView）**：文件管理、资源展示、挂载路径管理
- **控制窗口（ControlView）**：播放控制、进度显示、播放列表
- **IPC通信**：新增挂载路径相关的IPC消息
- **状态管理**：资源分类、筛选、挂载路径状态

---

## 📁 影响范围

### 需要修改的文件

#### 渲染进程（Renderer）
- `src/renderer/src/views/MainView.vue` - 完全重构
- `src/renderer/src/views/ControlView.vue` - 部分重构（时间显示、布局优化）
- `src/renderer/src/style.css` - 更新全局样式

#### 需要创建的文件

**组件（Components）**
- `src/renderer/src/components/Sidebar.vue` - 侧边栏组件
- `src/renderer/src/components/SidebarSection.vue` - 侧边栏分组组件
- `src/renderer/src/components/SidebarItem.vue` - 侧边栏项组件
- `src/renderer/src/components/MountPathItem.vue` - 挂载路径项组件
- `src/renderer/src/components/VideoCard.vue` - 视频卡片组件
- `src/renderer/src/components/VideoGrid.vue` - 视频网格组件
- `src/renderer/src/components/SearchBox.vue` - 搜索框组件
- `src/renderer/src/components/Toolbar.vue` - 工具栏组件
- `src/renderer/src/components/ViewToggle.vue` - 视图切换组件
- `src/renderer/src/components/SourceBadge.vue` - 来源标签组件
- `src/renderer/src/components/ProgressBar.vue` - 进度条组件（ControlView）

**Composables**
- `src/renderer/src/composables/useMediaLibrary.ts` - 媒体库管理
- `src/renderer/src/composables/useMountPaths.ts` - 挂载路径管理
- `src/renderer/src/composables/useResourceFilter.ts` - 资源筛选
- `src/renderer/src/composables/useVideoPlayer.ts` - 视频播放控制（ControlView）

**类型定义**
- `src/renderer/src/types/media.ts` - 媒体资源类型
- `src/renderer/src/types/mount.ts` - 挂载路径类型

#### 主进程（Main Process）
- `src/main/application/command/ipcHandlers.ts` - 新增挂载路径IPC处理

---

## 🎨 UI组件设计

### 组件层次结构

```
MainView
├── Header
│   ├── Logo
│   ├── SearchBox
│   └── SettingsButton
├── MainContentWrapper
│   ├── Sidebar
│   │   ├── SidebarSection (资源)
│   │   │   ├── SidebarItem (全部资源)
│   │   │   ├── SidebarItem (本地文件)
│   │   │   ├── SidebarItem (网络资源)
│   │   │   └── SidebarItem (NAS存储)
│   │   ├── SidebarSection (挂载路径)
│   │   │   └── MountPathItem (多个)
│   │   └── SidebarSection (最近)
│   │       ├── SidebarItem (最近播放)
│   │       ├── SidebarItem (收藏)
│   │       └── SidebarItem (播放列表)
│   └── ContentAreaWrapper
│       ├── Toolbar
│       │   ├── ToolbarLeft (添加文件、添加URL、挂载路径)
│       │   └── ToolbarRight (ViewToggle)
│       └── ContentArea
│           ├── ContentHeader
│           └── VideoGrid
│               └── VideoCard (多个)
│                   ├── VideoThumbnail
│                   ├── VideoPlayOverlay
│                   └── VideoInfo
│                       ├── VideoTitle (含SourceBadge)
│                       └── VideoMeta
```

### 组件详细设计

#### 1. Sidebar.vue
**职责**：侧边栏容器，管理所有侧边栏分组

**Props**：
- `activeFilter: 'all' | 'local' | 'network' | 'nas' | string` - 当前选中的筛选器
- `mountPaths: MountPath[]` - 挂载路径列表
- `selectedMountPath: string | null` - 选中的挂载路径

**Events**：
- `filter-change` - 筛选器改变
- `mount-path-select` - 选择挂载路径
- `mount-path-add` - 添加挂载路径
- `mount-path-remove` - 移除挂载路径
- `mount-path-refresh` - 刷新挂载路径

#### 2. MountPathItem.vue
**职责**：显示单个挂载路径项

**Props**：
- `mountPath: MountPath` - 挂载路径数据
- `active: boolean` - 是否选中

**Events**：
- `select` - 选择该路径
- `refresh` - 刷新扫描
- `remove` - 移除挂载

**数据结构**：
```typescript
interface MountPath {
  id: string
  path: string
  resourceCount: number
  lastScanned?: Date
}
```

#### 3. VideoCard.vue
**职责**：显示单个视频资源卡片

**Props**：
- `video: MediaResource` - 视频资源数据
- `viewMode: 'grid' | 'list'` - 视图模式

**Events**：
- `play` - 播放视频
- `context-menu` - 右键菜单

**数据结构**：
```typescript
interface MediaResource {
  id: string
  name: string
  path: string
  source: 'local' | 'network' | 'nas' | 'mounted'
  mountPath?: string  // 如果是挂载路径，显示路径
  duration?: number
  size?: number
  thumbnail?: string
}
```

#### 4. VideoGrid.vue
**职责**：视频网格/列表容器

**Props**：
- `videos: MediaResource[]` - 视频列表
- `viewMode: 'grid' | 'list'` - 视图模式
- `loading: boolean` - 加载状态

**Events**：
- `video-play` - 播放视频
- `video-select` - 选择视频

#### 5. SourceBadge.vue
**职责**：显示资源来源标签

**Props**：
- `source: 'local' | 'network' | 'nas' | 'mounted'` - 来源类型

#### 6. ProgressBar.vue (ControlView)
**职责**：进度条组件

**Props**：
- `currentTime: number` - 当前时间
- `duration: number` - 总时长
- `buffered?: number` - 缓冲进度

**Events**：
- `seek` - 跳转时间

---

## 🏗️ 实现方案

### 数据流设计

```
用户操作
  ↓
UI组件
  ↓
Composable (状态管理)
  ↓
IPC消息
  ↓
主进程处理
  ↓
IPC广播
  ↓
UI更新
```

### 状态管理设计

#### useMediaLibrary.ts
```typescript
export function useMediaLibrary() {
  const resources = ref<MediaResource[]>([])
  const activeFilter = ref<'all' | 'local' | 'network' | 'nas' | string>('all')
  const selectedMountPath = ref<string | null>(null)
  const viewMode = ref<'grid' | 'list'>('grid')
  const searchQuery = ref('')
  
  // 筛选后的资源
  const filteredResources = computed(() => {
    // 根据activeFilter和searchQuery筛选
  })
  
  // 添加资源
  const addResource = (resource: MediaResource) => {}
  
  // 移除资源
  const removeResource = (id: string) => {}
  
  // 设置筛选器
  const setFilter = (filter: string) => {}
  
  return {
    resources,
    activeFilter,
    selectedMountPath,
    viewMode,
    searchQuery,
    filteredResources,
    addResource,
    removeResource,
    setFilter
  }
}
```

#### useMountPaths.ts
```typescript
export function useMountPaths() {
  const mountPaths = ref<MountPath[]>([])
  
  // 添加挂载路径
  const addMountPath = async (path: string) => {
    // IPC: mount-path-add
    // 自动扫描资源
  }
  
  // 移除挂载路径
  const removeMountPath = (id: string) => {
    // IPC: mount-path-remove
  }
  
  // 刷新扫描
  const refreshMountPath = (id: string) => {
    // IPC: mount-path-refresh
  }
  
  return {
    mountPaths,
    addMountPath,
    removeMountPath,
    refreshMountPath
  }
}
```

### IPC通信设计

#### 新增IPC消息

**渲染进程 → 主进程**
- `mount-path-add` - 添加挂载路径
  - 参数：`{ path: string }`
  - 返回：`{ id: string, path: string, resources: MediaResource[] }`
  
- `mount-path-remove` - 移除挂载路径
  - 参数：`{ id: string }`
  
- `mount-path-refresh` - 刷新扫描挂载路径
  - 参数：`{ id: string }`
  - 返回：`{ resources: MediaResource[] }`
  
- `scan-directory` - 扫描目录
  - 参数：`{ path: string }`
  - 返回：`{ resources: MediaResource[] }`

**主进程 → 渲染进程**
- `mount-path-added` - 挂载路径已添加
  - 参数：`{ mountPath: MountPath, resources: MediaResource[] }`
  
- `mount-path-removed` - 挂载路径已移除
  - 参数：`{ id: string }`
  
- `mount-path-scanned` - 挂载路径扫描完成
  - 参数：`{ id: string, resources: MediaResource[] }`

---

## 📝 实现步骤

### 阶段 1：基础组件开发（2-3天）✅

#### 1.1 创建类型定义 ✅
- [x] 创建 `src/renderer/src/types/media.ts`
  - ✅ 定义 `MediaResource` 接口
  - ✅ 定义资源来源类型 `ResourceSource`
  - ✅ 定义资源筛选类型 `ResourceFilter`
  - ✅ 定义视图模式类型 `ViewMode`
- [x] 创建 `src/renderer/src/types/mount.ts`
  - ✅ 定义 `MountPath` 接口

#### 1.2 创建基础组件 ✅
- [x] 创建 `SourceBadge.vue` - 来源标签组件
- [x] 创建 `SearchBox.vue` - 搜索框组件
- [x] 创建 `ViewToggle.vue` - 视图切换组件
- [x] 创建 `Toolbar.vue` - 工具栏组件

#### 1.3 创建侧边栏组件 ✅
- [x] 创建 `SidebarItem.vue` - 侧边栏项组件
- [x] 创建 `SidebarSection.vue` - 侧边栏分组组件
- [x] 创建 `MountPathItem.vue` - 挂载路径项组件
- [x] 创建 `Sidebar.vue` - 侧边栏主组件

### 阶段 2：视频展示组件（2-3天）✅

#### 2.1 创建视频卡片组件 ✅
- [x] 创建 `VideoCard.vue` - 视频卡片组件
  - ✅ 缩略图显示（使用emoji占位）
  - ✅ 播放按钮覆盖层（悬停显示）
  - ✅ 来源标签集成
  - ✅ 悬停效果（阴影、边框高亮）
  - ✅ 支持网格和列表两种视图模式

#### 2.2 创建视频网格组件 ✅
- [x] 创建 `VideoGrid.vue` - 视频网格/列表组件
  - ✅ 网格视图（响应式布局）
  - ✅ 列表视图
  - ✅ 空状态显示
  - ✅ 加载状态显示

#### 2.3 创建内容区域组件 ✅
- [x] 创建 `ContentHeader.vue` - 内容标题组件
- [x] 创建 `ContentArea.vue` - 内容区域组件

### 阶段 3：Composables开发（2-3天）✅

#### 3.1 媒体库管理 ✅
- [x] 创建 `useMediaLibrary.ts`
  - ✅ 资源列表管理（添加/删除/批量添加）
  - ✅ 筛选逻辑（按来源、挂载路径）
  - ✅ 搜索功能（按名称和路径）
  - ✅ 视图模式管理
  - ✅ 统计信息计算

#### 3.2 挂载路径管理 ✅
- [x] 创建 `useMountPaths.ts`
  - ✅ 挂载路径列表管理
  - ✅ 添加挂载路径（IPC通信）
  - ✅ 移除挂载路径
  - ✅ 刷新扫描
  - ✅ 初始化挂载路径列表

#### 3.3 资源筛选 ✅
- [x] 创建 `useResourceFilter.ts`
  - ✅ 筛选逻辑（按来源、挂载路径）
  - ✅ 搜索筛选
  - ✅ 统计信息计算

### 阶段 4：主窗口重构（3-4天）✅

#### 4.1 重构MainView.vue ✅
- [x] 更新模板结构
  - ✅ Header区域（Logo、搜索框、设置按钮）
  - ✅ Sidebar区域（集成Sidebar组件）
  - ✅ Content区域（集成Toolbar、ContentArea组件）
- [x] 集成所有组件
- [x] 实现交互逻辑
  - ✅ 文件选择处理
  - ✅ URL添加处理
  - ✅ 挂载路径管理
  - ✅ 资源播放
  - ✅ 筛选和搜索
- [x] 应用新样式
  - ✅ 深蓝灰色系配色
  - ✅ 现代化布局

#### 4.2 样式更新 ✅
- [x] 更新全局样式（style.css）
- [x] 应用新的配色方案
- [x] 响应式设计

### 阶段 5：控制窗口优化（2天）⚠️

#### 5.1 优化ControlView.vue ✅
- [x] 调整时间显示位置（移到进度条下方）
  - ✅ 时间显示在进度条下方
  - ✅ 左对齐当前时间，右对齐总时长
  - ✅ 移除时间分隔符 `/`
- [x] 优化控制栏布局
  - ✅ 移除中间的control-center
  - ✅ 优化按钮布局和间距
  - ✅ 添加静音按钮功能
- [x] 应用新样式
  - ✅ 应用新的配色方案
  - ✅ 优化进度条样式
  - ✅ 优化按钮样式和悬停效果

#### 5.2 创建进度条组件 ❌
- [ ] 创建 `ProgressBar.vue`
  - ❌ 未创建独立组件（功能已在ControlView中实现）
- [ ] 集成到ControlView
  - ❌ 未使用独立组件

**说明**: 进度条功能已在ControlView中实现，但未创建独立组件。根据实际需求，可以后续考虑是否提取为独立组件。

### 阶段 6：IPC通信实现（2-3天）✅

#### 6.1 主进程IPC处理 ✅
- [x] 在 `ipcHandlers.ts` 中添加挂载路径处理
  - ✅ `select-mount-path` - 选择挂载路径对话框
  - ✅ `mount-path-add` 处理
  - ✅ `mount-path-remove` 处理
  - ✅ `mount-path-refresh` 处理
  - ✅ `get-mount-paths` - 获取挂载路径列表
  - ✅ `scan-directory` 处理

#### 6.2 目录扫描功能 ✅
- [x] 创建 `mountPathService.ts` - 挂载路径管理服务
  - ✅ 递归扫描视频文件
  - ✅ 支持常见视频格式（mp4, avi, mkv, mov等）
  - ✅ 提取文件信息（大小）
  - ✅ 返回资源列表
  - ✅ 挂载路径管理（添加/移除/刷新）
  - ✅ IPC事件广播

#### 6.3 IPC广播 ✅
- [x] 实现挂载路径相关的事件广播
  - ✅ `mount-path-added` - 挂载路径已添加
  - ✅ `mount-path-removed` - 挂载路径已移除
  - ✅ `mount-path-scanned` - 挂载路径扫描完成
  - ✅ `mount-paths-updated` - 挂载路径列表更新

### 阶段 7：测试与优化（2-3天）⏳

#### 7.1 功能测试 ⏳
- [ ] 测试资源添加/删除
- [ ] 测试挂载路径功能
- [ ] 测试筛选功能
- [ ] 测试搜索功能
- [ ] 测试播放功能

#### 7.2 UI测试 ⏳
- [ ] 测试响应式布局
- [ ] 测试交互效果
- [ ] 测试样式一致性

#### 7.3 性能优化 ⏳
- [ ] 虚拟滚动（如果资源很多）
- [ ] 图片懒加载
- [ ] 防抖搜索

---

## ⚠️ 注意事项

### Electron进程边界
- 所有文件系统操作必须在主进程完成
- 目录扫描在主进程执行，通过IPC返回结果
- 不能直接在主窗口访问文件系统

### 性能考虑
- 大量资源时使用虚拟滚动
- 目录扫描使用异步处理，避免阻塞UI
- 图片缩略图使用懒加载

### 状态管理
- 使用Vue 3 Composition API管理状态
- 避免在组件间直接传递大量数据
- 使用computed属性优化筛选性能

### 错误处理
- 挂载路径不存在时的处理
- 扫描失败时的错误提示
- IPC通信失败的处理

### 兼容性
- 确保Windows和macOS都能正常工作
- 路径分隔符处理（Windows使用`\`，macOS使用`/`）

---

## 🧪 测试方案

### 功能测试

#### 1. 资源管理测试
- [ ] 添加本地文件
- [ ] 添加URL资源
- [ ] 删除资源
- [ ] 搜索资源

#### 2. 挂载路径测试
- [ ] 添加挂载路径
- [ ] 自动扫描资源
- [ ] 刷新扫描
- [ ] 移除挂载路径
- [ ] 选择挂载路径筛选

#### 3. 筛选功能测试
- [ ] 全部资源筛选
- [ ] 本地文件筛选
- [ ] 网络资源筛选
- [ ] 挂载路径筛选

#### 4. 播放功能测试
- [ ] 双击播放本地文件
- [ ] 双击播放URL
- [ ] 双击播放挂载路径资源
- [ ] 播放列表切换

### UI测试

#### 1. 布局测试
- [ ] 侧边栏显示正常
- [ ] 内容区域显示正常
- [ ] 响应式布局（不同窗口大小）

#### 2. 交互测试
- [ ] 悬停效果
- [ ] 点击效果
- [ ] 动画过渡

#### 3. 样式测试
- [ ] 配色方案正确
- [ ] 字体大小合适
- [ ] 间距合理

### 性能测试

#### 1. 大量资源测试
- [ ] 100+ 资源显示性能
- [ ] 1000+ 资源显示性能
- [ ] 虚拟滚动效果

#### 2. 扫描性能测试
- [ ] 大目录扫描（1000+文件）
- [ ] 扫描进度显示
- [ ] 扫描不阻塞UI

---

## 📊 实现进度总览

| 阶段 | 任务 | 预计时间 | 实际状态 | 完成度 |
|------|------|----------|----------|--------|
| 阶段1 | 基础组件开发 | 2-3天 | ✅ 已完成 | 100% |
| 阶段2 | 视频展示组件 | 2-3天 | ✅ 已完成 | 100% |
| 阶段3 | Composables开发 | 2-3天 | ✅ 已完成 | 100% |
| 阶段4 | 主窗口重构 | 3-4天 | ✅ 已完成 | 100% |
| 阶段5 | 控制窗口优化 | 2天 | ⚠️ 部分完成 | 90% |
| 阶段6 | IPC通信实现 | 2-3天 | ✅ 已完成 | 100% |
| 阶段7 | 测试与优化 | 2-3天 | ⏳ 待开始 | 0% |
| **总计** | | **15-21天** | **85%完成** | **85%** |

**说明**: 
- 阶段5的ProgressBar组件未创建独立组件，但功能已在ControlView中实现
- 阶段7测试与优化待开始

---

## 🐛 已知问题

### 1. IPC通信
- **问题**: 挂载路径服务的主窗口引用需要在窗口创建后设置
- **当前方案**: 使用setTimeout延迟设置
- **建议**: 在VideoPlayerApp中监听窗口创建事件，主动设置主窗口引用

### 2. 目录扫描
- **问题**: 大目录扫描可能较慢，没有进度提示
- **当前方案**: 异步扫描，不阻塞UI
- **建议**: 添加扫描进度事件，在UI中显示进度条

### 3. 文件信息获取
- **问题**: 文件信息（时长）需要通过播放器获取
- **当前方案**: 仅获取文件大小
- **建议**: 后续可以通过MPV获取视频时长

### 4. 资源管理
- **问题**: 挂载路径的资源ID生成需要确保唯一性
- **当前方案**: 使用时间戳+随机字符串
- **建议**: 使用文件路径hash作为ID，确保唯一性

### 5. 进度条组件
- **问题**: 计划中的ProgressBar组件未创建
- **当前方案**: 进度条功能直接在ControlView中实现
- **建议**: 根据实际需求决定是否提取为独立组件

---

## 📝 后续优化建议

### 性能优化
1. **虚拟滚动**: 当资源数量 > 100时，实现虚拟滚动提升性能
2. **图片缩略图缓存**: 实现缩略图生成和缓存机制
3. **搜索防抖**: 搜索输入添加防抖，减少不必要的筛选计算

### 功能增强
1. **右键菜单**: 实现资源右键菜单（删除、属性、添加到播放列表等）
2. **拖拽排序**: 支持拖拽调整资源顺序
3. **批量操作**: 支持批量选择、删除、添加到播放列表
4. **资源收藏**: 实现收藏功能，在侧边栏显示收藏列表

### 用户体验
1. **加载动画**: 添加资源加载、扫描等操作的加载动画
2. **错误提示**: 添加友好的错误提示（Toast通知）
3. **操作确认**: 添加删除、移除挂载路径等操作的确认对话框
4. **快捷键**: 添加常用操作的快捷键支持

### 代码优化
1. **组件提取**: 考虑将进度条提取为独立组件（如计划中所述）
2. **类型安全**: 完善IPC通信的类型定义
3. **错误处理**: 完善错误处理机制，添加错误边界

---

## 📊 文件清单

### 已创建的文件

**类型定义** (2个):
- `src/renderer/src/types/media.ts`
- `src/renderer/src/types/mount.ts`

**组件** (12个):
- `src/renderer/src/components/SourceBadge.vue`
- `src/renderer/src/components/SearchBox.vue`
- `src/renderer/src/components/ViewToggle.vue`
- `src/renderer/src/components/Toolbar.vue`
- `src/renderer/src/components/SidebarItem.vue`
- `src/renderer/src/components/SidebarSection.vue`
- `src/renderer/src/components/MountPathItem.vue`
- `src/renderer/src/components/Sidebar.vue`
- `src/renderer/src/components/VideoCard.vue`
- `src/renderer/src/components/VideoGrid.vue`
- `src/renderer/src/components/ContentHeader.vue`
- `src/renderer/src/components/ContentArea.vue`

**Composables** (3个):
- `src/renderer/src/composables/useMediaLibrary.ts`
- `src/renderer/src/composables/useMountPaths.ts`
- `src/renderer/src/composables/useResourceFilter.ts`

**主进程服务** (1个):
- `src/main/application/services/mountPathService.ts`

### 已修改的文件

**视图** (2个):
- `src/renderer/src/views/MainView.vue` - 完全重构
- `src/renderer/src/views/ControlView.vue` - 部分重构

**样式** (1个):
- `src/renderer/src/style.css` - 更新全局样式

**IPC处理** (1个):
- `src/main/application/command/ipcHandlers.ts` - 添加挂载路径相关处理

---

## 🎯 下一步

1. **测试功能**：运行应用，测试所有功能是否正常工作
2. **修复问题**：根据测试结果修复发现的问题
3. **性能优化**：根据实际使用情况优化性能
4. **完善功能**：实现待完成的功能（如ProgressBar组件、测试等）

---

**注意**：主要功能已经实现，但还需要测试和优化才能投入使用。
