{
  "name": "mpv-player",
  "version": "1.0.0",
  "description": "MPV Player built with Electron, Vue and TypeScript",
  "main": "./out/main/main.js",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "serve:hls": "node serve-video.js --dir videos --port 8081",
    "serve": "node serve-video.js",
    "serve:8080": "node serve-video.js --port 8080",
    "build:native": "node-gyp rebuild --directory native",
    "rebuild:native": "node-gyp rebuild --directory native",
    "clean:native": "node-gyp clean --directory native",
    "package": "npm run build && npm run build:native && electron-builder",
    "package:mac": "npm run build && npm run build:native && electron-builder --mac",
    "dist": "npm run build && npm run build:native && electron-builder --mac --arm64",
    "test:domain": "echo '🧪 领域模型测试方法：\\n\\n方法1（推荐）：使用 IPC 命令\\n  1. 运行: npm run dev\\n  2. 在渲染进程控制台运行: window.electronAPI.send(\\'test-semantic-refactoring\\')\\n  3. 查看主进程控制台输出\\n\\n方法2：开发模式自动测试\\n  运行: NODE_ENV=development npm run dev\\n  测试会自动运行，查看主进程控制台\\n\\n详细说明请查看: docs/guides/TESTING_GUIDE.md'"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.25",
    "@vitejs/plugin-vue": "^5.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^26.4.0",
    "electron-vite": "^2.0.0",
    "node-addon-api": "^7.1.1",
    "node-gyp": "^10.3.1",
    "typescript": "5.3.3",
    "vite": "^5.0.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "vue-tsc": "^1.8.27"
  },
  "dependencies": {
    "element-plus": "^2.13.1"
  }
}
